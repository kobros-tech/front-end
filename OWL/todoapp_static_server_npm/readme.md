To run the static NPM server, firstly install the server:

```
$ npm install
```

If you find security issues run this:

```
$ npm audit fix --force
$ npm fund
```

Finally run the server:

```
$ npm run serve
```
